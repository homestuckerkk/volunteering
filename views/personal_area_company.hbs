    <div class="all">
    <div class="imag">
        <img class='image' src="/images/загружено.jpg">
        <button>Изменить фото профиля</button>
    </div>
    <div class="info">
        <h1>{{name}}</h1>
        <h1>{{email}}</h1>
        <h1>description</h1> 
        <input type="text">
        <div class="buttons">
            <a href=""><button>Забыли пароль?</button></a>
            <a href="form"><button>Создать мероприятие</button></a>
        </div>
    </div>
    <div><hr></div>
    </div>
   <div class="activity">
        <h1>Мероприятия</h1>
        {{#each rows}}
            {{> window name=name address=address date=date time=time}}
        {{/each}}
    </div>
